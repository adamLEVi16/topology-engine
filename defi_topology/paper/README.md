# Preprint source

`main.tex` is a self-contained, arXiv-ready LaTeX manuscript (only standard
packages: geometry, amsmath, natbib, graphicx, booktabs, hyperref, xcolor,
caption). `rq1_profile.png` is the sole figure. `main.pdf` is the compiled copy.

## Compile
- **Overleaf** (easiest): upload `main.tex` + `rq1_profile.png`, hit recompile.
- **Local**: `pdflatex main.tex && pdflatex main.tex` (twice, for cross-refs).

## Before posting (fill these in)
1. Author name, department, and email (currently placeholders).
2. Complete the three arXiv reference author lists (flagged `verify author list`).
3. arXiv q-fin typically requires an **endorsement** for a first-time submitter —
   arrange via your faculty mentor.
